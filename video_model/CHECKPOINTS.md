# Checkpoints

Pretrained checkpoints are not shipped in this repo. Download per the README:

    wget https://videopolicy.cs.columbia.edu/assets/checkpoints.zip

Then place the extracted `checkpoints/` folder under `video_model/`.
